# amazon_nodejs
creating bots using nodeJs

# Steps for how to run this bots
1. npm install. After this all libery will be installed 
2. node amazon.js, this command will run the node js application 
3. Enter links to monitor (separated by commas): , this command will come after node amazon.js and you will eneters amazon products links 
to monitor the bots 
4. You can install pm-2 with this commond first in your terminal npm install pm2 -g. Navigate to the directory where your Node.js script (your-app.js) is located using the cd command. For example: cd /path/to/your-app-directory run you node file nohup node your-app.js &
5. After running this command, you'll see some output, and your Node.js script will continue running in the background. You can close the terminal, and the script will keep running.